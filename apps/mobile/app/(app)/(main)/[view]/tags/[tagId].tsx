// A view narrowed by a tag: `/links/tags/<id>`. The two filters have always
// combined, so they are one screen with both segments read off the route.
export { ArchiveScreen as default } from "@/features/workspace/archive-screen";
